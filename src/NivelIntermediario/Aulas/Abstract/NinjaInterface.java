package NivelIntermediario.Aulas.Abstract;

public interface NinjaInterface {
    // Atributos não são colocadas por conta de ser todos tipo FINAL na interface e não é possível setar valor depois.
    // Ele é obrigatoriamente abstrato mesmo sem a declaração abstract
    public void arremessarShuriken();
}
